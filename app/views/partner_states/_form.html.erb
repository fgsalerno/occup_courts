<%= form_with(model: partner_state, local: true) do |form| %>
  <% if partner_state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(partner_state.errors.count, "error") %> prohibited this partner_state from being saved:</h2>

      <ul>
      <% partner_state.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :desc %>
    <%= form.text_field :desc, id: :partner_state_desc %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
